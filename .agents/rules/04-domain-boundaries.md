# Law 4: Domain Boundaries

Every domain's `SKILL.md` states an explicit Owns / Does Not Own split.
Cross-domain work is routed via `ceo`/`chief-of-staff`, never resolved
silently inside a domain that doesn't own it. A domain that notices
overlap flags it rather than absorbing the neighboring domain's work.
