# Trail Schema

The trail is the ordered, replayable execution history of one PMCR-O cycle
(or sequence of looped cycles) scoped to one domain. Every domain-scoped
dispatch from `orchestrator` writes here.

## Directory Layout

```
<repo_path>/.pmcro/trails/<domain>/<uuid>/
├── 00-deps.json            # written once, by orchestrator, cycle 01 step 1
├── 00-frame.json          # written once, by planner, cycle 01 only
├── 01-plan.jsonl
├── 01-make.jsonl
├── 01-check.jsonl
├── 01-reflect.jsonl
├── 02-plan.jsonl          # only present if cycle 01 looped
├── 02-make.jsonl
├── 02-check.jsonl
├── 02-reflect.jsonl
├── 03-plan.jsonl          # EC-009 caps this at 3 cycles max
├── 03-make.jsonl
├── 03-check.jsonl
├── 03-reflect.jsonl
└── disposition.json        # written once, by orchestrator, on seal
```

`<uuid>` is generated fresh per trail via `scripts/new_trail_id.py`
(python3, `uuid.uuid4()`) -- never reused, never hand-typed.

## 00-deps.json

Written once, by `orchestrator`, immediately after step 1 (Resolve
dependencies), before the trail's frame. Records dependency-resolver's
actual output so later roles -- especially checker -- can verify resolution
happened and verify what it returned, rather than trusting it happened.

```json
{
  "requested_by": "orchestrator",
  "needs": ["planner", "maker", "checker", "reflector"],
  "resolved": [
    {"name": "planner", "version": "1.0.0", "pmcro_provides": "planner"},
    {"name": "maker", "version": "1.0.0", "pmcro_provides": "maker"},
    {"name": "checker", "version": "1.0.0", "pmcro_provides": "checker"},
    {"name": "reflector", "version": "1.0.0", "pmcro_provides": "reflector"}
  ],
  "domain_scope_path": "<path to the target domain's domain-scope/SKILL.md>",
  "resolved_at": "<ISO 8601>"
}
```

## 00-frame.json

Written once, by `planner`, on cycle 01 only. Immutable after write --
later cycles in the same trail do not edit this file, only add new
`NN-*.jsonl` sets. If a later cycle's context genuinely contradicts the
original frame, that contradiction belongs in that cycle's `reflect.jsonl`
as an open hypothesis, not as a silent edit to the frame.

```json
{
  "trail_id": "<uuid>",
  "domain": "<one of the 10 C-Suite domains>",
  "true_intent": "<free-text, as given by the invoking command>",
  "created_at": "<ISO 8601>",
  "requested_by": "<command that dispatched this cycle, e.g. /ceo:approve-initiative>"
}
```

## NN-plan.jsonl / NN-make.jsonl / NN-check.jsonl / NN-reflect.jsonl

Each is JSON Lines -- one JSON object per line, one line per discrete
item (a plan step, a make action, a check finding, a reflect note). This
keeps each role's output independently appendable and independently
diffable, and lets Checker point at a specific plan-step line rather than
"the plan" as an undifferentiated blob.

Minimum shape per line (roles add role-specific fields on top):

```json
{"cycle": "NN", "seq": 1, "role": "plan|make|check|reflect", "content": "...", "refs": ["<optional line refs into a prior file>"]}
```

`check.jsonl`'s last line is always the disposition signal `orchestrator`
and `reflector` read:

```json
{"cycle": "NN", "seq": "final", "role": "check", "disposition": "pass|fail", "failing_ids": ["<seq of failing findings, if fail>"]}
```

## disposition.json

Written once, by `orchestrator`, when the trail seals (loop stops, one way
or another):

```json
{
  "trail_id": "<uuid>",
  "sealed_at": "<ISO 8601>",
  "final_cycle": "NN",
  "disposition": "accept|needs-approval|reject|needs-revision",
  "reason": "<short, specific -- not just restating the disposition>"
}
```

Sealed trails are immutable. Errors discovered after sealing get logged as
open hypotheses in a *new* trail's frame, never as an edit to a sealed
one's files.
