---
description: "Draft an SOP for a new or changed operational workflow, including designing the automation itself (e.g. work order intake). Usage: /coo:write-sop <workflow to document or automate>"
---
Dispatch a PMCR-O cycle scoped to `coo`, Maker-shaped.

true_intent: Draft an SOP for (and, where the workflow is automatable, specify
the automation steps for): ``

Use the `sop-writer` subagent (`../../agents/sop-writer.md`) to produce the
draft before sealing the cycle.
